# 准生产数据准备记录

> 执行日期：2026-07-27
>
> 环境：本机隔离准生产数据库
>
> 状态：名单、关系和人员账号已准备完成；尚未创建可登录的准生产管理员

## 1. 隔离边界

- 新建数据库：`makeup_booking_staging_20260727`。
- 现有开发数据库未覆盖、未清空、未迁移数据。
- 目标数据库创建前检查同名库；仅允许在全新空库中继续执行。
- 41 个数据库迁移全部成功应用。
- 数据迁移使用无登录凭证的专用审计主体；该主体不能登录网页。
- 私密账号发放表和执行上下文保存在 `data/private/`，由 Git 忽略。

本数据库只是本机准生产验收环境，不等于正式生产数据库，也不能替代生产环境的备份、恢复、监控和容量验收。

## 2. 名单导入结果

| 数据 | 源数据量 | 成功写入 | 说明 |
|---|---:|---:|---|
| 主播 | 1,235 | 1,235 | 主播编号保持唯一 |
| 化妆师 | 58 | 58 | 昵称 `33` 按文本保留 |
| 运营 | 123 | 123 | 已按最新名单去重 |
| 主播—运营源关系 | 1,234 | 1,151 | 仅建立可精确匹配且场地一致的关系 |
| 固定主播历史参考 | 340 | 0 | 按约定不导入数据库 |

关系处理结果：

- 83 条关系因无法精确同场地匹配而留空。
- 1 位主播在关系源表中没有对应行。
- 合计 84 位主播暂未分配运营，不执行模糊匹配。

## 3. 人员账号结果

| 角色 | 账号数量 | 登录账号规则 |
|---|---:|---|
| 主播 | 1,235 | 使用主播编号 |
| 化妆师 | 58 | `MA0001` 起顺序生成，登录时不区分大小写 |
| 运营 | 123 | `OP0001` 起顺序生成，登录时不区分大小写 |
| 合计 | 1,416 | 全部要求首次登录修改密码 |

数据库对账结果：

- 已绑定人员：1,416。
- 未绑定人员：0。
- 登录账号冲突：0。
- 强制首次改密：1,416。
- 不同密码哈希：1,416；即使初始密码相同，每个账号也使用独立盐值。
- 账号发放表不包含初始密码，账号和初始密码必须分开渠道发放。

主播编号后续通过管理后台修改时，登录账号在同一事务中同步修改；内部用户 ID、历史记录和预约不变。若新编号或新登录账号冲突，整次修改回滚。

## 4. 已生成的私密交付物

`data/private/outputs/staging-accounts-20260727/化妆部预约系统-准生产账号发放表-20260727.xlsx`

工作簿包含“使用说明、主播账号、化妆师账号、运营账号”四张表，已经完成：

- 行数和角色数量核对；
- 工作表、表格区域和关键字段检查；
- Excel 公式错误扫描；
- 四张表的渲染预览检查；
- 初始密码与数据库连接信息泄漏检查。

该文件包含人员姓名和登录账号，只能内部受控发放，不应提交 Git、发送到公共群或公开网盘。

## 5. 下一步与停止条件

继续真实业务 UAT 前还需要：

1. 由项目负责人确定准生产管理员的登录名、显示名称和独立强密码。
2. 管理员登录后创建松江、现厂客服账号并核对场地权限。
3. 58 位化妆师首次登录后设置班次；未设置班次前不能被预约。
4. 选取两场地的主播、运营、化妆师和客服代表执行核心业务 UAT。
5. 核对 84 位未分配运营的主播，确认后再通过后台建立关系。
6. UAT 通过后才进入正式生产资源准备、正式导入和单场地灰度。

任何真实预约、批量账号发放或生产部署都不能仅凭本记录自动执行。
