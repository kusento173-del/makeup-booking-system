# 当前迭代问题清单

> 记录日期：2026-07-29
>
> 本清单持续记录本轮真实业务验收中发现的问题。每一项必须完成代码修复、自动化回归和 Git 提交后，才标记为完成。

## 待处理

- [x] 主播、化妆师、运营手机端增加“个人信息”，展示姓名、角色、场地和网页账号；主播账号即主播编号。
- [x] 化妆师班次页明确展示当前班次和最近一次修改申请的完整详情。
- [x] 将班次、审批等页面中的英文状态统一转换为业务人员能理解的中文。
- [x] 将“数据状态冲突，请刷新后重试”等泛化提示替换为可执行、可理解的具体说明。
- [x] 修复桌面端排班看板主内容区域无法滚动的问题，并增加真实长列表滚动回归。
- [x] 客服和管理员可以直接设置、修改或取消固定主播关系。
- [x] 客服和管理员可以查看并直接设置化妆师班次。
- [x] 修复主播、化妆师、运营查看历史排班时出现“请求内容不正确”的问题。
- [x] 对主播、化妆师、运营和客服可见页面进行英文枚举值及泛化错误提示全量检查。
- [x] 完成 API、网页端、权限、构建及关键端到端回归，确认本轮修改未引入新增问题。

## 已完成说明

- 化妆师班次和加班申请状态已改为中文；班次重复提交、已有待审核申请、状态已变化、班次时间不正确等情况会给出具体处理方法。其余页面的英文枚举值仍按清单继续排查。
- 历史排班默认查询最近 30 天，并允许自行选择任意不超过 31 天的历史区间；查询结果会自动分页加载，不再只保留前 100 条。
- 后台直接设置固定关系沿用原固定申请的排班冲突校验，并在同一个数据库事务内完成申请记录和审批，保留完整操作历史。
- 运营固定申请、化妆师班次与加班、后台操作记录等页面不再直接显示英文状态码；操作记录的筛选条件和主要字段也改为中文。
- 原统一“数据状态冲突”已按预约、请假、加班、班次、固定关系、临时不可排班、导出和人员资料分别给出具体处理方法。
- 排班看板使用 12 名化妆师的长列表做真实浏览器回归，确认主内容区可独立滚动且看板内容不再被裁切。
- 全仓格式、代码规范、类型检查、单元测试和生产构建全部通过：网页端 47 项、API 412 项、后台任务 7 项。
- 五角色真实数据库端到端验收 6 项全部通过，覆盖角色权限隔离、预约、固定申请与审批、请假恢复、局部不可排、班次与加班审批、三端固定关系以及主播编号变更。
- 最新迁移已增量应用到准生产数据库，服务健康检查全部正常；迁移前后人员数据保持一致：主播 1235、化妆师 59、运营 123、有效主播—运营关系 1151。

## 已知非阻断项

- 五角色验收构建和准生产构建会出现前端单文件超过 500 KB 的体积提示，不影响当前功能；正式公网发布前应单独做按页面拆包优化。
- PostgreSQL 驱动在并发测试中会输出一条兼容性弃用提示，当前测试与业务结果正常；升级到 `pg@9` 前需要整理同一连接上的并行查询。

## 验收纪律

1. 按上述顺序分阶段实现，避免多个高风险修改混在一次提交中。
2. 每一阶段至少补充对应单元测试或端到端回归。
3. 每一阶段测试通过后单独提交 Git，再继续下一阶段。
4. 涉及排班、固定关系和班次的修改必须同时验证角色权限、场地隔离和并发冲突。
