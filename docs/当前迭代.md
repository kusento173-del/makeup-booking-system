# 当前迭代

> 阶段：M2 主数据、身份与权限
>
> 状态：进行中
>
> 最后更新：2026-07-21

## 当前目标

建立场地、主播、化妆师、运营及其归属关系的权威主数据，并完成最小身份绑定、角色和场地权限闭环。

## 已完成

- 九份正式产品与技术文档已建立。
- 五类角色的可点击原型已建立并私有发布。
- 排班看板已经按业务可读性重新整理，支持显示化妆师可排班时间。
- 真实人员 Excel 已放入受保护且被 Git 忽略的数据目录。
- 已确认讲师不进入第一阶段。
- 已确认无法匹配运营的主播关系留空。
- 已确认历史化妆师固定主播名单仅保留参考，不导入数据库。
- 已建立文档索引、开发入口和按任务读取机制。
- 已确认手机端采用 Taro＋React 微信小程序，不采用微信内网页。
- 已完成 `M1-BASELINE-01`：初始化 pnpm workspace、Node.js 与 pnpm 版本约束、TypeScript 严格模式、ESLint、Prettier、Vitest 和环境变量样例。
- 已建立 API、Worker、Web 管理后台和微信小程序四个最小应用骨架。
- 四个应用均已通过类型检查、单元测试和生产构建；工程基线不包含预约业务逻辑。
- pnpm 依赖构建脚本采用显式允许清单，锁文件已通过供应链策略校验。
- 已完成 `M1-ENV-02`：PostgreSQL 18.4 与 Redis 8.8.0 使用 Compose 固定版本、健康检查、本机端口绑定和持久卷运行。
- PostgreSQL 连接、Redis 密码认证、容器重启和 Compose 停启后的数据卷持久化均已验证。
- 已完成 `M1-DATABASE-03`：接入 Prisma 7.9.0，建立 PostgreSQL Schema、迁移目录和数据库操作命令。
- 首个迁移仅启用 `btree_gist`；已在随机临时空库从零执行成功并确认没有业务表，临时库随后已清理。
- 已完成 `M1-CI-04`：GitHub Actions 在推送和拉取请求中安装锁定依赖、启动独立基础设施、执行空库迁移和全部项目检查。
- CI Action 固定到完整提交哈希并使用最小只读权限；完整命令链已在本机随机隔离 Compose 项目中通过，临时容器和数据卷已清理。
- 已完成 `M1-SECURITY-05`：将 Taro 间接依赖的 Swiper 从 11.1.15 收敛到已修复的 12.1.2，并通过漏洞复现用例、小程序生产构建和完整项目检查验证兼容性。
- CI 已加入生产依赖高危漏洞门禁和 Git 已跟踪敏感文件检查；真实数据目录、环境配置、密钥、数据库备份和常见导出文件一旦被 Git 跟踪会直接失败。
- 完整依赖审计保留为显式命令；Taro 4.2.1 构建链仍含上游尚未发布可用修复版本或需要跨主版本替换的风险项，不以静默忽略换取绿色结果。
- 已完成 `M1-LOCAL-GATE-06`：接入 Husky 与 lint-staged，提交前只检查暂存文件；建立 Playwright 端到端冒烟，验证构建后的管理后台页面和 API 健康接口。
- CI 已在完整质量检查后安装锁定的 Chromium 并运行端到端冒烟；本地使用现有 Chrome，不把浏览器二进制或测试产物提交到 Git。
- M1 工程与环境基线达到退出标准，可以进入 M2。
- 已完成 `M2-MASTER-DATA-01`：建立场地、主播、化妆师、运营和主播—运营有效关系五张主数据表，使用 PostgreSQL 18 原生 UUIDv7、外键、状态检查、部分唯一索引和日期排斥约束保护核心不变量。
- 松江、现厂和无锡作为基础场地随迁移初始化；没有导入任何真实人员、历史固定主播或无法匹配的运营关系。
- 主数据迁移已在随机临时空库从零执行，Prisma 漂移检查无差异；数据库约束检查已加入 CI，并确认测试事务回滚后四类业务表为空。
- API 已建立主数据模块和统一匹配文本标准化服务，覆盖 Unicode NFKC、首尾空格和英文大小写；尚未开放无权限保护的 CRUD 接口。
- 已完成 `M2-IDENTITY-DATA-02`：建立账号、外部身份、密码凭据、角色和会话五张身份数据表，并为主播、化妆师和运营主档增加唯一账号绑定能力。
- 身份迁移已在随机临时空库从零执行，Prisma 漂移检查无差异；手机号保护字段、外部身份唯一性、客服场地范围、有效角色唯一性、会话令牌哈希和历史数据禁止删除均有数据库约束检查。
- API 已建立默认拒绝的权限策略边界并通过测试：客服仅能访问所属场地，管理员可访问全场地，其他角色不能凭场地参数扩大权限，本人范围必须匹配已验证账号。首次账号绑定方式尚未确认，因此本任务不开放登录或绑定接口。
- 已完成 `M2-AUDIT-DATA-03`：建立只追加操作审计表、对象/场地/操作人查询索引和审计条目工厂；数据库触发器拒绝日志修改与删除，服务端与数据库双重拦截密码、令牌、完整手机号和微信身份字段。
- 审计迁移已在随机临时空库从零执行，三组数据库约束检查与 Prisma 漂移检查通过；普通用户操作必须记录用户与实际角色，系统任务必须使用无用户的 `SYSTEM` 角色。

## 当前决策门

| 决策 | 暂定基线 | 最晚确认时间 | 状态 |
|---|---|---|---|
| 手机端形态 | Taro＋React 微信小程序 | M1 工程初始化前 | 已确认 |
| 首次账号绑定 | 一次性绑定码或手机号组合 | M2 身份模块前 | 待业务确认 |
| 微信通知渠道 | 小程序订阅消息优先 | M7 通知模块前 | 待业务与资质确认 |
| 通知失败短信补发 | 第一阶段不做，系统内失败可追踪 | M7 验收前 | 暂定 |
| 加班申请过期 | 不自动过期 | M3 加班模块前 | 暂定 |
| Excel 默认导出 | 客服按场地；管理员可分场地或合并 | M6 导出模块前 | 暂定 |
| 生产云、域名和备案主体 | 未选择 | M1 部署基线前 | 待确认 |

未到对应模块的决策不会阻塞无关开发，但同一能力只实现一套正式方案，不用多套代码同时兼容未确定选项。

## 当前技术风险

- `pnpm security:audit:all` 仍会报告 Taro 4.2.1 仅在开发和构建阶段使用的间接依赖风险；其中 `decompress` 和 `html-minifier` 的审计建议修复版本尚未在 npm 发布，其余项需要上游升级或单独完成跨主版本兼容验证。生产依赖高危门禁当前通过。
- 当前仓库未配置 GitHub 远程地址，因此 CI 已完成本地等价验证，但尚无 GitHub 托管运行记录。

## 下一任务

任务编号：`M2-DATABASE-ACCESS-04`

结果：建立 API 的 Prisma 数据库连接生命周期和事务边界，将主数据写入与审计追加组织为同一事务，为后续有权限保护的人员维护服务提供可测试的持久化基础；暂不开放业务 CRUD 接口。

外部准备状态：后续需要创建或指定 GitHub 私有仓库，才能取得首次 GitHub 托管 CI 运行记录；这不阻塞本地安全门禁开发。

## 本阶段退出标准

- 权限矩阵核心测试全部通过。
- 客服不能跨场地访问或操作数据。
- 主播不能通过请求参数切换为其他主播。
- 运营权限严格按目标日期有效关系计算。
- 主播编号和有效化妆师昵称唯一约束有效。
- 所有关键写操作都有不可删除的审计记录。
