# 当前迭代

> 阶段：M5 固定预约闭环
>
> 状态：进行中
>
> 最后更新：2026-07-22

## 当前目标

建立仅由运营发起、客服或管理员审核的固定预约规则，并按工作日、请假、加班和单次占用生成每日预约实例；固定规则与单次预约共享档期、状态和实际服务化妆师口径。

## 已完成

- 九份正式产品与技术文档已建立。
- 五类角色的可点击原型已建立并私有发布。
- 排班看板已经按业务可读性重新整理，支持显示化妆师可排班时间。
- 真实人员 Excel 已放入受保护且被 Git 忽略的数据目录。
- 已确认讲师不进入第一阶段。
- 已确认无法匹配运营的主播关系留空。
- 已确认历史化妆师固定主播名单仅保留参考，不导入数据库。
- 已建立文档索引、开发入口和按任务读取机制。
- 已确认手机端采用 Taro＋React 微信小程序，不采用微信内网页。
- 已完成 `M1-BASELINE-01`：初始化 pnpm workspace、Node.js 与 pnpm 版本约束、TypeScript 严格模式、ESLint、Prettier、Vitest 和环境变量样例。
- 已建立 API、Worker、Web 管理后台和微信小程序四个最小应用骨架。
- 四个应用均已通过类型检查、单元测试和生产构建；工程基线不包含预约业务逻辑。
- pnpm 依赖构建脚本采用显式允许清单，锁文件已通过供应链策略校验。
- 已完成 `M1-ENV-02`：PostgreSQL 18.4 与 Redis 8.8.0 使用 Compose 固定版本、健康检查、本机端口绑定和持久卷运行。
- PostgreSQL 连接、Redis 密码认证、容器重启和 Compose 停启后的数据卷持久化均已验证。
- 已完成 `M1-DATABASE-03`：接入 Prisma 7.9.0，建立 PostgreSQL Schema、迁移目录和数据库操作命令。
- 首个迁移仅启用 `btree_gist`；已在随机临时空库从零执行成功并确认没有业务表，临时库随后已清理。
- 已完成 `M1-CI-04`：GitHub Actions 在推送和拉取请求中安装锁定依赖、启动独立基础设施、执行空库迁移和全部项目检查。
- CI Action 固定到完整提交哈希并使用最小只读权限；完整命令链已在本机随机隔离 Compose 项目中通过，临时容器和数据卷已清理。
- 已完成 `M1-SECURITY-05`：将 Taro 间接依赖的 Swiper 从 11.1.15 收敛到已修复的 12.1.2，并通过漏洞复现用例、小程序生产构建和完整项目检查验证兼容性。
- CI 已加入生产依赖高危漏洞门禁和 Git 已跟踪敏感文件检查；真实数据目录、环境配置、密钥、数据库备份和常见导出文件一旦被 Git 跟踪会直接失败。
- 完整依赖审计保留为显式命令；Taro 4.2.1 构建链仍含上游尚未发布可用修复版本或需要跨主版本替换的风险项，不以静默忽略换取绿色结果。
- 已完成 `M1-LOCAL-GATE-06`：接入 Husky 与 lint-staged，提交前只检查暂存文件；建立 Playwright 端到端冒烟，验证构建后的管理后台页面和 API 健康接口。
- CI 已在完整质量检查后安装锁定的 Chromium 并运行端到端冒烟；本地使用现有 Chrome，不把浏览器二进制或测试产物提交到 Git。
- M1 工程与环境基线达到退出标准，可以进入 M2。
- 已完成 `M2-MASTER-DATA-01`：建立场地、主播、化妆师、运营和主播—运营有效关系五张主数据表，使用 PostgreSQL 18 原生 UUIDv7、外键、状态检查、部分唯一索引和日期排斥约束保护核心不变量。
- 松江、现厂和无锡作为基础场地随迁移初始化；没有导入任何真实人员、历史固定主播或无法匹配的运营关系。
- 主数据迁移已在随机临时空库从零执行，Prisma 漂移检查无差异；数据库约束检查已加入 CI，并确认测试事务回滚后四类业务表为空。
- API 已建立主数据模块和统一匹配文本标准化服务，覆盖 Unicode NFKC、首尾空格和英文大小写；尚未开放无权限保护的 CRUD 接口。
- 已完成 `M2-IDENTITY-DATA-02`：建立账号、外部身份、密码凭据、角色和会话五张身份数据表，并为主播、化妆师和运营主档增加唯一账号绑定能力。
- 身份迁移已在随机临时空库从零执行，Prisma 漂移检查无差异；手机号保护字段、外部身份唯一性、客服场地范围、有效角色唯一性、会话令牌哈希和历史数据禁止删除均有数据库约束检查。
- API 已建立默认拒绝的权限策略边界并通过测试：客服仅能访问所属场地，管理员可访问全场地，其他角色不能凭场地参数扩大权限，本人范围必须匹配已验证账号。首次账号绑定已确认使用一次性绑定码，登录和绑定接口将在身份服务完成后开放。
- 已完成 `M2-AUDIT-DATA-03`：建立只追加操作审计表、对象/场地/操作人查询索引和审计条目工厂；数据库触发器拒绝日志修改与删除，服务端与数据库双重拦截密码、令牌、完整手机号和微信身份字段。
- 审计迁移已在随机临时空库从零执行，三组数据库约束检查与 Prisma 漂移检查通过；普通用户操作必须记录用户与实际角色，系统任务必须使用无用户的 `SYSTEM` 角色。
- 已完成 `M2-DATABASE-ACCESS-04`：建立共享数据库包，使用 Prisma 7 PostgreSQL 驱动适配器和单例连接池；API 统一管理连接与关闭生命周期，并提供交互式事务边界。
- API 健康检查已接入真实数据库查询；审计仓储强制复用调用方事务，后续业务写入与审计追加可以原子提交或共同回滚。生成型 Prisma Client 和构建产物不进入 Git。
- 已完成 `M2-MASTER-DATA-QUERY-05A`：场地、主播、化妆师和运营查询按已验证账号、人员绑定、客服场地及主播—运营有效关系从数据库计算范围；查询服务不接受可用于扩大权限的场地参数。
- 已完成 `M2-MASTER-DATA-CREATE-05B1`：管理员可新增场地，客服仅能在所属场地新增主播、化妆师、运营及同场地主播—运营关系；创建与脱敏审计在同一数据库事务中完成。
- 已完成 `M2-MASTER-DATA-UPDATE-05B2`：场地、主播、化妆师、运营及主播—运营关系支持带行版本的修改、停用和关系结束；并发覆盖、跨场地写入和迁入停用场地会被拒绝，修改前后快照与业务写入在同一事务中审计。
- 已完成 `M2-HOST-QUALIFICATION-HISTORY-05B3`：主播创建时记录初始预约资格，资格变化时在主档修改和操作审计的同一事务中追加状态历史；历史表由数据库约束状态转换并禁止更新或删除，普通资料修改不会产生伪资格历史。
- 已完成 `M2-IMPORT-DATA-06`：建立人员与主播—运营关系导入批次、暂存行和业务主档来源追踪；数据库约束模板类型、文件摘要、批次生命周期、行定位和预检结果，并明确拒绝历史固定主播名单导入。
- 已完成 `M2-BINDING-CODE-DATA-07A`：首次绑定方案冻结为一次性绑定码，并建立绑定目标、场地、摘要、有效期、尝试次数、消费、撤销和不可删除历史约束；不开发手机号验证分支。
- 已完成 `M2-BINDING-CODE-SERVICE-07B`：管理员可全场地、客服可在本站为未绑定且有效的人员签发或重新签发绑定码；明文只返回一次，服务端使用 HMAC-SHA256 摘要和恒定时间比较，错误尝试可提交计数，正确消费使用行版本防止并发复用，发码审计不包含明文或摘要。
- 已完成 `M2-WECHAT-BINDING-07C`：建立微信 `auth.code2Session` 适配边界、同身份事务锁、待绑定账号和 10 分钟高熵绑定凭证；一次性绑定码可原子关联账号、微信身份、人员档案和角色，多角色账号返回明确的角色选择数据，OpenID 与 `session_key` 不进入客户端或审计日志。
- 已完成 `M2-AUTH-SESSION-07D`：正式会话绑定本次选定角色，使用 15 分钟签名访问令牌与 30 天高熵刷新令牌；刷新令牌只保存摘要并原子轮换，业务鉴权重新校验账号、会话和角色，支持单会话退出与全端退出且保留不可删除历史。
- 已完成 `M2-AUTH-ROLE-SELECTION-07E1`：多角色登录使用 5 分钟高熵一次性选择凭证，凭证只保存摘要并绑定已验证账号；消费时重新校验账号与目标角色，避免客户端仅凭账号 ID 和角色 ID 越权创建会话。
- 已完成 `M2-AUTH-HTTP-07E2`：开放微信登录、一次性绑定、角色选择、刷新、当前身份、单会话退出和全端退出接口；请求采用严格字段白名单，受保护接口从服务端验证后的访问令牌生成身份上下文，统一错误响应不泄露内部异常信息。
- 已完成 `M2-AUTH-CONTRACT-RATE-LIMIT-07E3`：认证接口已生成 `/openapi.json` 契约；微信登录、绑定、角色选择和刷新使用 Redis 原子计数实现跨 API 实例一致的限流，键名只含标识摘要，超限返回 429 与 `Retry-After`。Redis 异常时认证入口默认拒绝，健康检查同时验证 PostgreSQL 与 Redis。
- 已完成 `M2-BACKOFFICE-AUTH-08A`：客服与管理员可使用规范化登录名和 Argon2id 密码登录；连续失败 5 次锁定 15 分钟，错误响应不区分账号、密码或账号状态。登录后改密要求验证当前密码、禁止复用，并在同一事务中更新密码、撤销全部会话和追加脱敏审计。
- 已建立首次管理员初始化命令：密码仅在交互式终端隐藏输入，不接受命令参数或管道，不进入 Git、日志或审计；存在未撤销管理员角色时命令拒绝再次创建。
- 已完成 `M2-INITIAL-ADMIN-08B`：项目负责人已在本机完成首个管理员创建；非敏感核验确认账号、管理员角色、密码身份、Argon2id 凭据和初始化审计各一条且互相一致，Codex 未读取登录名或密码。
- 已完成 `M2-MASTER-DATA-HTTP-09A`：开放受访问令牌保护的场地、主播、化妆师和运营查询接口；人员列表默认每页 50、最大 100，支持规范化搜索，主播—运营关系按上海业务日期判定。客户端不能提交场地等额外参数扩大范围，未登录返回 401、无权角色返回 403，OpenAPI 已包含完整路径和分页参数。
- 已完成 `M2-MASTER-DATA-HTTP-09B1`：开放场地、主播、化妆师、运营和主播—运营关系新增接口；写入身份由服务端根据当前登录态生成，请求执行严格字段校验，业务写入与审计记录保持同一事务。
- 已完成 `M2-MASTER-DATA-HTTP-09B2`：开放场地、主播、化妆师和运营的带行版本修改/停用接口，以及主播—运营关系的分页查询和结束接口；客服只能维护本站数据，关系管理只对客服和管理员开放。
- 已完成 `M2-BINDING-CODE-HTTP-10A`：开放受保护的一次性绑定码签发接口；管理员可处理全部场地，客服只能为本站尚未绑定且有效的主播、化妆师或运营发码，明文绑定码仅在签发响应中出现一次。
- 已完成 `M2-ACCOUNT-ROLE-HTTP-10B`：管理员可分页查询后台账号，创建客服/管理员密码账号，修改或停用账号，分配或撤销后台角色；停用账号和撤销角色会立即撤销相关会话，系统禁止停用或撤销最后一个有效管理员。
- 已完成 `M2-ADMIN-WEB-11A`：管理后台接入真实密码登录、多人角色选择、页签级会话保持、访问令牌校验/刷新和安全退出；登录页已通过桌面与 390px 窄屏视觉检查，正文和表单字号不低于 15px。
- 已完成 `M2-ADMIN-WEB-11B1`：管理后台接入场地、主播、化妆师、运营、主播—运营关系及后台账号列表；支持规范化搜索、每页 50 条分页、场地名称转换、空状态和错误状态，客服界面不显示管理员专属账号入口。
- 已完成 `M2-ADMIN-WEB-11B2A`：后台可新增场地、主播、化妆师、运营和后台账号；未绑定人员可直接生成仅显示一次的绑定码。人员列表只返回“是否已绑定”，不向前端暴露关联账号 ID。
- 已完成 `M2-ADMIN-WEB-11B2B`：后台可修改或停用场地及人员、建立或结束主播—运营关系，并可维护后台账号状态和管理员/分场地客服角色；所有修改使用行版本防止并发覆盖，停用、撤销和关系结束必须填写原因。
- 管理后台与 API 已提供 `pnpm dev:admin` 一键联调入口；API 开发模式使用 TypeScript 增量编译后的正式依赖注入元数据，避免热更新运行方式与生产构建行为不一致。
- 本地环境新增 `pnpm env:setup`：缺少认证密钥或仍使用模板值时生成独立的高熵随机值，只写入 Git 忽略的 `.env` 且不输出明文；已修复首次管理员账号验证成功后因本机认证密钥缺失而无法签发会话的问题。
- 已完成 `M2-ACCEPTANCE-12A`：项目负责人使用本机首个管理员账号完成真实登录，服务端成功签发有效会话；M2 权限、并发、数据库约束、审计、构建与端到端自动化门禁全部通过。后台具体用语和操作反馈继续随实际使用迭代，不阻塞 M3 领域开发。
- 已完成 `M3-SHIFT-DOMAIN-01`：建立无数据库依赖的班次时间规则，统一校验 ISO 工作日、15 分钟粒度、有/无午休顺序和日内边界；按左闭右开区间计算连续工作段，并验证完整服务时长不能跨午休或下班。边界与批量不变量测试已覆盖。
- 已完成 `M3-SHIFT-DATA-02`：建立不可变的化妆师班次版本和班次修改申请数据结构；数据库拒绝版本日期重叠、非法工作日和时间、历史版本修改或删除、跨化妆师引用及同一化妆师重复待审批申请。迁移已在本地现有库和随机隔离空库验证，全部约束检查与 Prisma 漂移检查通过。
- 已完成 `M3-SHIFT-SERVICE-03`：实现首次班次设置与当前班次查询服务；化妆师只能设置和查看本人，客服仅能操作本站，管理员可操作全部场地。首次设置使用上海业务日期，校验通过后在同一事务内抢占一次性配置权、更新主档、创建版本并追加脱敏审计，并发重复设置不会产生第二个版本。
- 已完成 `M3-SHIFT-HTTP-04`：开放受访问令牌保护的首次班次设置与当前完整班次查询接口；请求严格拒绝未知字段和伪造场地查询参数，化妆师、客服和管理员复用服务端权限范围。OpenAPI 已包含两条接口，未登录访问实测返回 401，班次参数错误、目标不存在和重复首次设置均有稳定且不泄露内部信息的错误响应。
- 已完成 `M3-SHIFT-CHANGE-05`：实现化妆师本人提交和撤回后续班次修改、本站客服或管理员审核的事务服务；申请最早次日生效，待审时不修改现有班次，禁止跨场地审核与提交人自审。审批通过使用行版本确保只有一次成功，先关闭旧版本再创建关联申请的新版本；驳回不触碰班次。预约表尚未建立，服务明确要求当前影响预约数为零，后续接入影响预览和取消时再替换该边界。
- 已完成 `M3-SHIFT-CHANGE-HTTP-06`：开放班次修改提交、按本人或后台场地范围分页查询、本人撤回和客服/管理员审核接口；客户端不能提交场地范围，日期、状态、分页、行版本和请求体均严格解析。OpenAPI 四条新增路径已在本地服务实测存在，未登录查询返回 401；服务测试累计 152 项通过。
- 已完成 `M3-SHIFT-DIRECT-07`：本站客服与管理员可填写原因直接代改班次，最早次日生效并使用期望版本号防止覆盖并发修改；直接代改与审批通过复用同一版本替换内核，统一关闭旧版本、创建新版本和追加前后快照审计。受保护接口已加入 OpenAPI 并实测未登录返回 401。
- 已完成 `M3-LEAVE-OVERTIME-DATA-08`：建立主播/化妆师请假与化妆师单日加班数据结构；数据库约束请假对象二选一、连续不超过七日、有效日期不重叠、取消状态字段一致，以及加班时间合法、审批状态一致和同一化妆师同日最多一条待审或已批准记录。迁移已在现有库和随机隔离空库从零验证，约束检查与 Prisma 漂移检查通过。
- 已完成 `M3-LEAVE-SERVICE-09`：实现主播与化妆师本人请假影响预览、确认生效和截止前取消服务；日期严格限制在明日起未来七日，客服不能代请假，普通用户只能取消本人且角色匹配的记录，管理员仅可填写原因进行数据纠错。影响预览、请假写入和取消均保留后续预约表接入点；当前影响数必须为零，取消不会恢复预约。
- 已完成 `M3-LEAVE-HTTP-10`：开放本人请假影响预览、确认和截止前取消接口；日期、确认影响数、行版本、原因和记录 ID 均严格解析，未知字段直接拒绝。接口从登录态解析本人身份，客服不能借请求参数代请假；稳定错误响应与 OpenAPI 契约已补齐，三条路径实测未登录均返回 401。
- 已完成 `M3-OVERTIME-SERVICE-11`：实现化妆师本人提交和撤回非工作日加班、本站客服或管理员审核，以及客服或管理员直接添加已批准加班的事务服务。目标日期限制为明日起未来七日，并按该日期实际生效的班次版本判断常规非工作日；加班时间复用 15 分钟班次校验，跨场地、自审、重复有效申请和并发重复审核均被拒绝，所有状态变化与审计原子提交。预约表尚未建立，批准时要求当前影响预约数为零并保留后续重算接入点。
- 已完成 `M3-OVERTIME-HTTP-12`：加班申请支持按化妆师本人、客服所属场地或管理员全场地分页查询，并开放本人提交与撤回、后台审核和直接批准接口；日期、班次、状态、分页、UUID、行版本、决定和文本均严格解析，客户端不能提交场地范围。OpenAPI 五条路径已在本地服务实测存在且未登录均返回 401，稳定错误响应不暴露内部异常。
- 已完成 `M3-AVAILABILITY-13`：建立化妆师指定日期可工作时间的唯一领域查询，统一处理人员与场地状态、目标日期生效班次、有效请假、常规工作日和批准加班；结果明确区分常规班次、加班及五类不可工作原因，并返回扣除午休后的连续时间段。常规工作日不查询加班表，非工作日才追加一次查询；后续预约、固定生成和排班看板必须复用该服务。M3 的时间区间、权限、并发审批和可工作日期退出标准已全部满足。
- 已完成 `M4-APPOINTMENT-DATA-01`：建立第一阶段单次预约实例、上海业务日期、人员和场地快照、15 分钟时间范围、每日序号、取消/完成状态与改期血缘。数据库使用同场地复合外键、部分唯一索引和 GiST 排斥约束兜底跨场地、每日最多两次以及主播/化妆师时间冲突；已取消预约释放档期和序号，已完成预约保留历史占用。预约身份与时间不可原地修改、记录不可删除，合法状态变化必须递增行版本；现有库及临时空库从零执行 17 个迁移、约束检查和 Prisma 漂移检查均通过，临时库已删除。固定血缘将在 M5 固定表存在后以外键追加。
- 已完成 `M4-BOOKING-SLOTS-02`：建立明日起未来七日、15/30/45/60 分钟和上海业务时间转换的纯预约规则，并实现主播—化妆师指定日期可预约档位查询。查询复用统一化妆师可工作时间，校验主播资格、场地、请假和每日次数，扣除双方已有有效预约后只返回完整时长可容纳且不跨午休的 15 分钟起点；已有一次预约时返回二次确认标记，两次后不再返回档位。固定长期格将在 M5 接入同一查询边界。
- 已完成 `M4-BOOKING-RELIABILITY-DATA-03`：建立账号＋业务动作＋幂等键唯一的接口幂等记录，可保存请求摘要、完成响应和资源引用；建立事务外盒事件及事件类型＋聚合对象唯一约束，预约业务事务无需同步调用微信或队列。请求摘要、响应状态、资源成对关系、外盒载荷、投递状态和尝试次数均有数据库检查约束；现有库及临时空库从零执行 18 个迁移、全部约束和 Prisma 漂移检查通过，临时库已删除。
- 已完成 `M4-BOOKING-CREATE-04`：实现主播本人、目标日期当前运营、本站客服和管理员创建单次预约的事务服务。提交时在按主播、化妆师和幂等键排序的事务锁内重新校验日期、15 分钟时间、权限、主播资格及请假、同场地、化妆师统一可工作时间、完整工作区间、双方时间冲突和每日次数；第一次直接预约，第二次要求显式确认，第三次拒绝。幂等占位、预约及快照、脱敏审计、最小通知外盒和可重放响应在同一事务内完成，数据库唯一索引与 GiST 排斥约束继续作为最终并发防线。
- 已完成 `M4-BOOKING-HTTP-05`：开放受访问令牌保护的可预约档期查询与单次预约创建接口；查询和创建均从服务端登录态校验主播本人、目标日期当前运营、本站客服或管理员范围，不接受客户端场地或身份扩权。查询参数、请求体和 UUID 使用严格字段白名单，创建要求请求头幂等键；OpenAPI 已包含两条路径，二次确认、每日上限、不可工作、跨场地和档期抢占均有稳定且可操作的错误响应。两条接口已在本地运行服务实测存在，未登录访问均返回 401。
- 已完成 `M4-BOOKING-CANCEL-06`：实现主播本人、目标日期当前运营、本站客服和管理员取消预约的事务服务与受保护接口。主播和运营在预约当天上海时间 0 点后不能取消，客服和管理员可处理当日特殊情况但始终必须填写原因；目标日期运营关系和客服场地均从数据库重验。取消使用期望行版本，只允许 `BOOKED` 一次性变为 `CANCELLED`，释放数据库档期与每日序号，并在同一事务追加前后状态审计及最小通知外盒；已完成、已取消、越权和并发重复取消均被拒绝。接口已进入 OpenAPI，未登录实测返回 401。
- 已完成 `M4-BOOKING-RESCHEDULE-07`：实现预约改期和更换实际预约化妆师的单一事务服务与受保护接口。改期不原地覆盖时间或化妆师，而是把原预约变为 `CANCELLED`、创建新的 `SINGLE` 预约，并通过双向来源字段记录血缘；新预约预分配 UUID，使原预约取消原因可在同一事务中指向替代预约。事务按原/新主播、化妆师、日期和幂等键统一排序加锁，先处理重放，再重验原预约权限与截止时间、新日期运营关系、主播资格、双方请假、化妆师可工作时间、每日两次和档期冲突；新预约、两条脱敏审计、两端最小通知外盒和幂等响应任一步失败都会使原预约保持有效。接口已进入 OpenAPI，未登录实测返回 401。
- 已完成 `M4-BOOKING-CONCURRENCY-08`：新增每次创建、迁移并最终删除独立临时数据库的预约并发门禁，并接入统一 `db:check` 和 CI 数据库步骤。真实 PostgreSQL 验证 100 个不同主播并发抢同一化妆师档期仅 1 个成功，同一主播 100 个并发创建仅 2 个有效预约；取消后同档期与每日序号可安全复用。改期冲突事务回滚后原预约仍为 `BOOKED`，成功改期时原/新双向血缘、两条操作审计、两条通知外盒和幂等完成结果作为一个整体提交；测试结束确认不存在临时数据库残留。
- 已完成 `M4-APPOINTMENT-QUERY-09`：开放今日、明日、未来七日、最长 31 日指定区间和历史指定日期的预约分页查询。主播仅看本人，化妆师仅看本人实际服务预约，运营范围按每条预约业务日期落入的有效主播关系计算，客服仅看所属场地，管理员可全局查询；客户端不能提交场地扩权。结果统一返回主播、实际预约化妆师、场地和运营快照、单次来源、开始结束时间、时长、每日序号、改期来源和行版本；数据库仍为 `BOOKED` 但结束时间已过的预约对外即时显示 `COMPLETED`。接口已进入 OpenAPI，未登录实测返回 401。M4 的创建、二次提醒、每日上限、取消、改期、查询和并发退出标准已满足。
- 已完成 `M5-FIXED-DATA-01`：建立固定预约创建、变更、取消三类审核申请，审核后生效的固定规则，以及按 ISO 星期拆分的规则占用结构。申请记录目标主播、场地、当前规则、目标化妆师、星期集合、时间、时长和生效日期；固定规则默认无结束日期，只有批准的变更或取消申请才能结束。数据库使用同场地复合外键、部分唯一索引和四维 GiST 排斥约束，拒绝重复待审、同主播多条有效固定、化妆师或主播在相同星期/时间/有效日期重叠、与批准申请不一致的规则和非法状态转换；申请、规则与星期历史均不可删除或覆盖。每日预约已支持 `FIXED` 类型并强制关联同场地来源规则，`SINGLE` 预约禁止伪造固定血缘。现有库及随机临时空库从零执行 20 个迁移、全部数据库约束、并发门禁和 Prisma 漂移检查通过；历史固定主播名单未导入，固定业务表保持为空。
- 已完成 `M5-FIXED-AVAILABILITY-02`：开放固定预约可选时段与最早可持续开始日期查询。查询只允许目标主播在申请生效日的当前运营、本站客服或管理员使用，并从服务端重验主播资格、双方场地和人员状态；固定时间必须同时落入化妆师当前及已批准未来班次的常规工作日和完整工作区间，加班不会被误用作长期工作时间。正式固定规则及其他待审核固定申请会永久禁用重叠的星期档期；未来单次预约不会直接禁用，而是逐格返回冲突日期和最后一次冲突次日的最早开始日期。主播及化妆师已登记请假按所选星期展开为受影响日期提示，不会错误取消长期固定资格。接口使用严格字段白名单且不接受场地参数扩权。
- 已完成 `M5-FIXED-REQUEST-03`：实现仅由目标主播当前有效运营提交固定预约创建申请。星期、原因和幂等键在进入事务前统一规范化；事务按主播、化妆师、星期和 15 分钟格有序加锁，随后重新校验人员、场地、当前及已批准未来班次、正式固定、其他待审核固定、未来单次预约和最早开始日期。只有查询结果仍允许从所选日期持续固定时才写入 `PENDING` 申请，并在同一事务完成操作审计、通知外箱和 24 小时幂等结果。单次预约创建及档期查询已接入相同固定占用规则和兼容事务锁，因此固定申请审核期间会占住长期格，固定申请与单次预约并发竞争时按先到先得，失败一方不会留下半成品。
- 已完成 `M5-FIXED-QUERY-04A`：开放固定预约申请分页查询。运营可查看本人历史提交以及当前仍负责主播的申请，客服只能查看所属场地，管理员可查看全部；支持申请类型、状态和每页最多 100 条的严格筛选，不接受场地参数扩权。结果返回主播、化妆师、场地、提交运营、目标星期与时间、审核状态、审核意见和行版本等审核页面所需字段，历史申请不会因运营关系变更而从原提交人视图消失。
- 已完成 `M5-FIXED-REVIEW-04B`：开放本站客服和管理员的固定预约申请审核。审核使用请求 ID、期望行版本和独立事务锁，客服范围由服务端场地重验，驳回必须填写意见并在事务提交后释放待审核长期格。批准创建申请会锁定主播及化妆师的全部星期/15 分钟格，排除申请自身占位后重新计算人员、班次、正式固定、其他待审核固定、未来单次预约和最早开始日期；校验通过后先把申请原子变为 `APPROVED`，再建立长期规则及标准化星期占用，使待审核占位到正式占位之间没有可抢占空窗。审核结果、操作审计和通知外箱共同提交，过期生效日、并发覆盖、跨场地和最终数据库排斥冲突均被拒绝。
- 已完成 `M5-FIXED-GENERATION-DATA-05A`：为固定预约实例增加 `(固定规则, 业务日期)` 非空条件唯一索引。无论实例当前为已预约、已取消还是已完成，同一规则同一天都不能再次插入，因此请假或单日取消后的历史记录不会被滚动任务错误重建；普通单次预约的空固定规则字段不受影响。第 21 个迁移已应用到现有库，并通过固定业务约束、随机并发数据库门禁、迁移状态和 Prisma 零漂移检查。
- 已完成 `M5-FIXED-GENERATION-ENGINE-05B`：建立可重试的未来八日固定预约生成引擎。引擎按规则有效日期与 ISO 星期逐日处理，同时支持尚未到结束日的已结束规则；每个规则日期在独立短事务中锁定生成键、主播/化妆师当日档期和 15 分钟格，随后重新校验常规班次或已批准加班、双方请假、人员状态、完整工作区间、当日两次上限及实际时间冲突。生成实例标记为 `FIXED` 并关联来源规则，保存主播、实际预约化妆师、场地和当日运营快照，操作审计与通知外箱共同提交；已存在历史、不可工作、请假、次数上限和档期冲突按原因计数跳过，不覆盖原记录。
- 已完成 `M5-FIXED-GENERATION-WORKER-05C`：独立 Worker 启动即触发固定预约补齐，随后默认每 60 秒运行；同一进程上一轮未结束时跳过重叠轮次，API 侧规则日期事务锁和数据库唯一索引继续防止多 Worker 或重试重复生成。Worker 只承担调度，通过不进入 OpenAPI 的内部接口调用唯一生成引擎；内部令牌至少 32 位、恒定时间比较、本地由 `pnpm env:setup` 自动生成且不写入 Git，错误日志不包含令牌。接口已实测正确令牌返回未来八日生成结果、错误令牌返回 401；本地 Worker 已实际启动并完成首轮零数据补齐。
- 已完成 `M5-FIXED-CHANGE-CANCEL-06`：仅目标主播当前运营可以提交固定星期、时间、时长变更或取消固定申请；变更沿用原固定化妆师，更换长期固定化妆师必须先取消后重新申请。提交使用独立幂等范围和主播、规则、化妆师、星期、15 分钟格有序事务锁，变更可用性计算排除即将被替代的当前规则但仍检查其他正式固定、待审申请和未来单次预约；取消不虚构替代档期。本站客服或管理员批准后，旧规则及其星期占用在生效日原子结束，生效日及之后尚未执行的旧固定实例逐条转为已取消并保留来源申请、审核人、原因、审计和通知外箱；变更在同日建立新不可变规则，取消不建立替代规则。滚动生成器取得排班锁后会再次确认规则仍对目标日期有效，避免取消或变更竞态补出旧预约；排班查询现可正确返回 `FIXED` 与 `SINGLE` 类型。申请、规则、星期占用和预约历史均未删除或覆盖，单日取消、改期仍不会修改长期规则。
- 已完成 `M5-FIXED-SELF-SERVICE-07`：运营、本站客服和管理员可按各自数据范围查询主播的当前有效固定规则及唯一待审申请，响应直接提供规则 ID、实际固定化妆师、星期、时间、时长、生效日和行版本，运营无需从历史申请猜测当前版本。固定可用性查询支持携带当前规则 ID 进行变更预览，但服务端只允许排除属于目标主播的有效规则，不能借参数隐藏其他主播或化妆师的长期冲突。原提交运营可在审核前凭期望行版本撤回 `PENDING` 申请；撤回与客服审核共享申请事务锁，只有一方能成功，状态改为 `WITHDRAWN` 后长期格立即释放，申请、审计和通知外箱历史保留。M5 固定创建、查询、审核、滚动生成、变更、取消、撤回及固定/单次排班类型已完成 65 个 API 测试文件、308 条 API 用例、真实 PostgreSQL 固定历史约束和 100 路预约并发门禁验证，固定关系闭环达到退出标准。
- 已完成 `M6-SCHEDULE-BOARD-01`：建立只读客服排班看板接口，以预约表作为唯一排班事实，不复制可编辑排班数据。客服固定读取本站，管理员必须明确选择场地；支持任意历史日期以及今日至未来七日，不能借场地参数越权。单次查询批量返回场地、最后更新时间及按昵称稳定排序的化妆师行；每行包含常规班次或已批准加班来源、可工作区间、午休区间、请假/非工作日/未设置班次/停用原因，以及按开始时间排序的有效固定和单次预约。预约块直接返回主播编号和姓名、运营快照、15 分钟起止刻度、实际时长、每日序号、行版本和按当前时间计算的展示状态；已取消记录默认不占用时间轴。接口使用批量关系查询而不是逐化妆师请求，适配 50 至 100 名化妆师的看板读取。
- 已完成 `M6-SCHEDULE-WEB-02`：管理后台以排班看板作为默认入口并接入真实接口，统一沿用简洁、较大正文字号和紧密卡片布局。页面提供今日至未来七日快捷切换、可选历史日期、管理员场地选择、客服本站范围、手动刷新和最后更新时间；按化妆师分行固定展示可排班区间、午休、加班或不可工作原因，并将有效固定与单次预约按开始时间紧密排列，结构化展示时间、主播姓名和编号、时长、第二次预约及运营。加载、刷新、空数据、错误和重新加载状态完整；未加入拖拽、重复排班数据或装饰性文案。前端新增业务日期边界和接口参数测试，完整页面视觉验收待使用现有后台账号登录本地页面后执行。
- 已完成 `M6-EXPORT-DATA-03`：建立异步排班导出任务数据结构，明确单场地与全部场地范围、请求人和实际请求角色、排班日期、待处理/处理中/成功/失败状态、标准文件名与 MIME 类型、对象存储键、文件大小和 SHA-256、导出行数、失败摘要、处理时间及下载过期时间。数据库按状态强制字段完整性和时间顺序，只允许待处理进入处理中、再进入成功或失败，任务请求范围和终态历史不可修改或删除，行版本必须逐次递增；不安全文件名、空存储键、非法摘要及不完整结果会直接拒绝。第 22、23 个迁移已应用现有库，并在临时空库从零执行全部迁移、所有数据库约束和 100 路预约并发门禁后清理临时库。
- 已完成 `M6-EXPORT-API-04`：建立受保护的排班导出创建与列表接口。客服只能提交不带场地参数的单场地任务，服务端强制使用当前客服角色场地；管理员可明确选择有效单场地或全部有效场地，不能用范围与场地参数的矛盾组合。导出日期支持历史并限制未来七日，创建请求使用用户级幂等键和事务锁，重复重试返回原任务、不同请求复用键会拒绝；任务、操作日志、唯一异步外箱事件和幂等结果在同一事务提交。列表由管理员查看全部、客服仅查看本站且永远不包含全场地任务，响应只公开页面所需的状态、行数、文件名、失败和过期信息，不泄露对象存储键。下载能力只在任务成功且未过期时标记，实际文件签发等待存储适配器完成后开放。

## 当前决策门

| 决策 | 暂定基线 | 最晚确认时间 | 状态 |
|---|---|---|---|
| 手机端形态 | Taro＋React 微信小程序 | M1 工程初始化前 | 已确认 |
| 首次账号绑定 | 一次性绑定码；24 小时有效、最多尝试 5 次 | M2 身份模块前 | 已确认 |
| 微信通知渠道 | 小程序订阅消息优先 | M7 通知模块前 | 待业务与资质确认 |
| 通知失败短信补发 | 第一阶段不做，系统内失败可追踪 | M7 验收前 | 暂定 |
| 加班申请过期 | 不自动过期 | M3 加班模块前 | 暂定 |
| Excel 默认导出 | 客服按场地；管理员可分场地或合并 | M6 导出模块前 | 暂定 |
| 生产云、域名和备案主体 | 未选择 | M1 部署基线前 | 待确认 |

未到对应模块的决策不会阻塞无关开发，但同一能力只实现一套正式方案，不用多套代码同时兼容未确定选项。

## 当前技术风险

- `pnpm security:audit:all` 仍会报告 Taro 4.2.1 仅在开发和构建阶段使用的间接依赖风险；其中 `decompress` 和 `html-minifier` 的审计建议修复版本尚未在 npm 发布，其余项需要上游升级或单独完成跨主版本兼容验证。生产依赖高危门禁当前通过。
- 当前仓库未配置 GitHub 远程地址，因此 CI 已完成本地等价验证，但尚无 GitHub 托管运行记录。

## 下一任务

任务编号：`M6-EXPORT-WORKER-05`

结果：实现唯一的排班 Excel 生成内核和异步 Worker 消费。标准工作表保持当前可直接使用的四列格式，扩展工作表保存场地、编号、运营、时长、固定/单次和状态；只读取有效预约快照，防止公式注入，按场地和时间稳定排序。文件存储通过抽象接口隔离云厂商，本地开发使用受控临时存储，成功或失败按数据库状态机落库并支持安全重试。

外部准备状态：后续需要创建或指定 GitHub 私有仓库，才能取得首次 GitHub 托管 CI 运行记录；这不阻塞本地安全门禁开发。

## 本阶段退出标准

- 预约相关自动化用例全部通过。
- 100 个请求同时抢同一化妆师档期时只有一个成功。
- 主播每日最多两次预约无法通过并发或取消记录绕过。
- 改期任一步失败时原预约保持不变。
- 所有角色均不能新增、取消或改期当天预约。
- 主播本人、运营、客服和管理员的数据范围正确。
