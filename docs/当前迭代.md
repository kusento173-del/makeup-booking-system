# 当前迭代

> 阶段：M1 工程与环境基线
>
> 状态：进行中
>
> 最后更新：2026-07-21

## 当前目标

建立可复现、可测试、可持续集成的正式工程和本地开发环境，为 M2 主数据与身份权限开发提供稳定基线。

## 已完成

- 九份正式产品与技术文档已建立。
- 五类角色的可点击原型已建立并私有发布。
- 排班看板已经按业务可读性重新整理，支持显示化妆师可排班时间。
- 真实人员 Excel 已放入受保护且被 Git 忽略的数据目录。
- 已确认讲师不进入第一阶段。
- 已确认无法匹配运营的主播关系留空。
- 已确认历史化妆师固定主播名单仅保留参考，不导入数据库。
- 已建立文档索引、开发入口和按任务读取机制。
- 已确认手机端采用 Taro＋React 微信小程序，不采用微信内网页。
- 已完成 `M1-BASELINE-01`：初始化 pnpm workspace、Node.js 与 pnpm 版本约束、TypeScript 严格模式、ESLint、Prettier、Vitest 和环境变量样例。
- 已建立 API、Worker、Web 管理后台和微信小程序四个最小应用骨架。
- 四个应用均已通过类型检查、单元测试和生产构建；工程基线不包含预约业务逻辑。
- pnpm 依赖构建脚本采用显式允许清单，锁文件已通过供应链策略校验。

## 当前决策门

| 决策 | 暂定基线 | 最晚确认时间 | 状态 |
|---|---|---|---|
| 手机端形态 | Taro＋React 微信小程序 | M1 工程初始化前 | 已确认 |
| 首次账号绑定 | 一次性绑定码或手机号组合 | M2 身份模块前 | 待业务确认 |
| 微信通知渠道 | 小程序订阅消息优先 | M7 通知模块前 | 待业务与资质确认 |
| 通知失败短信补发 | 第一阶段不做，系统内失败可追踪 | M7 验收前 | 暂定 |
| 加班申请过期 | 不自动过期 | M3 加班模块前 | 暂定 |
| Excel 默认导出 | 客服按场地；管理员可分场地或合并 | M6 导出模块前 | 暂定 |
| 生产云、域名和备案主体 | 未选择 | M1 部署基线前 | 待确认 |

未到对应模块的决策不会阻塞无关开发，但同一能力只实现一套正式方案，不用多套代码同时兼容未确定选项。

## 下一任务

任务编号：`M1-ENV-02`

结果：建立 PostgreSQL、Redis 的 Docker Compose 本地环境和健康检查，为数据库迁移与队列接入做准备，不实现业务功能。

前置：当前电脑尚未安装 Docker Desktop；执行本任务前需要先完成安装并启动 Docker Desktop。

## 本阶段退出标准

- 新开发者按照 README 能启动全部本地依赖和应用骨架。
- 空数据库可以执行全部迁移。
- 格式、Lint、类型检查、测试和四端构建在 CI 中稳定通过。
- 示例环境变量不包含真实信息。
