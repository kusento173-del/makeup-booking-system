# 当前迭代

> 更新时间：2026-07-30
>
> 阶段：M10 已完成；腾讯云单机受限试运行部署收尾

## 当前结论

- 五类角色已经统一使用 `apps/admin-web` 响应式网页。
- 主播、运营和化妆师使用手机布局；客服和管理员使用电脑布局。
- 统一账号密码登录、多角色选择、首次改密、会话刷新和权限校验已经落地。
- 微信小程序、Taro、微信登录、一次性绑定码及相关数据库表已经删除。
- API、Worker、PostgreSQL、Redis、业务规则和现有数据不因客户端迁移而改变。
- 数据库迁移 `20260727123500_remove_wechat_binding_tables` 已在本地应用。
- 独立本机准生产库已经完成 43 个迁移、最新名单导入和 1,416 个人员账号初始化；开发库未改动。
- 账号全部要求首次改密，私密账号发放表不含初始密码。
- 现有开发管理员已安全同步到准生产库；导入用迁移主体的管理员角色已撤销。
- 松江客服宋雨、现厂客服周婕及其原有登录凭证已同步，场地权限核对通过。
- 本机准生产 API、网页和 Worker 已在独立端口启动，数据库、Redis 和导出目录均与开发环境隔离。
- “账号与角色”已显示五类真实有效角色并支持筛选；人员角色只读且由对应人员档案维护，客服和管理员角色仍在账号页维护。
- 主播预约资格已统一为“正常 / 取消资格”；取消资格必须设置当天至一个月内的截止日期，截止次日自动恢复正常。
- 化妆师、运营和客服不再提供可人工切换的停用状态；未删除人员统一为正常。
- 管理员可业务删除除管理员外的人员；客服只能删除本站主播、化妆师和运营。删除会撤销登录和会话，主播或化妆师的未来预约与固定规则受控取消，历史和审计保留。
- 人员页支持正常、已删除筛选，主播另支持预约资格筛选，账号页支持角色和账号状态组合筛选。
- 人员和账号页现在默认只展示正常记录；删除后从默认列表消失，只有主动选择“已删除”才显示。
- 已删除主播编号重新新增时恢复原内部档案、资料和网页账号角色，历史预约与审计保持连续。
- 人员和账号管理页的页头筛选控件已统一为紧凑尺寸；桌面端不再拉伸成整行，窄屏自动换行且不溢出。
- 客服后台已开放“账号与角色”：只显示本站账号和只读管理员账号，可维护本站非管理员账号及客服角色，不能创建、编辑或授予管理员账号与管理员角色。
- 主播网页账号固定使用主播编号；化妆师和运营的新账号分别由系统按 `MA`、`OP` 历史最大序号加一生成，生成后不可修改且已删除序号不复用，并发开通由数据库事务锁串行保护。
- 客服和管理员桌面后台已改为固定顶部栏与左侧导航，人员名单和业务内容只滚动右侧主区域；菜单过高时使用独立滚动，手机窄屏导航保持原有行为。
- 十日业务仿真已完成 8,052 条预约记录，覆盖五类角色、固定与单次预约、请假、改期、取消、人员删除和并发抢占；最终无化妆师重叠、主播重叠、每日超两次或跨场地预约。
- 首期部署方案确定为单台 Linux 轻量服务器运行 PostgreSQL、Redis、API、Worker 和网页；目标规格 4 核 4 GB、40 GB SSD、3 Mbps，后续按监控阈值扩容。
- 单机编排、HTTP 与 HTTPS 网关、43 个迁移、数据库与 Redis 健康检查及备份校验已在隔离数据卷通过。
- 暂无域名时使用 Let’s Encrypt 短期公网 IP 证书自动续期，确保试运行登录链路加密；该措施不替代 ICP 备案。
- 腾讯云上海 4 核 4 GB、3 Mbps、40 GB 服务器已安装 Docker Compose，配置 2 GB Swap；PostgreSQL、Redis、API、Worker 和 Gateway 均为健康状态。
- 目标服务器已部署 Git 提交 `473d470`，43 个数据库迁移已应用；API、Worker 和网页生产镜像使用同一提交标签。
- 最新名单已正式写入目标数据库：主播 1,037、化妆师 58、运营 124；986 条主播—运营关系精确匹配，51 条无法精确匹配的关系留空，342 条历史固定名单未导入。
- 目标服务器已创建 1,219 个人员账号并绑定全部人员档案；加上首个管理员共 1,220 个有效账号。人员账号首次登录必须改密。
- 导入前、导入后数据库备份均已生成并通过 `pg_restore` 隔离恢复核对；恢复库核对完成后已删除。
- 数据库每日 02:30 自动备份并保留最近 7 天；IP 证书每 12 小时检查续期，证书未变化时不重启网关。
- 服务器内部 HTTPS、证书和 HTTP→HTTPS 跳转正常；公网 443 数据包尚未到达服务器，仍需核对腾讯云轻量服务器防火墙规则。

关键提交：

- `aaa16dc`：统一网页账号认证。
- `04a5958`：响应式手机网页。
- `03900c5`：删除微信小程序客户端。
- `f47d4e3`：删除微信认证和绑定运行时。

## 本轮验收

- 本轮全仓格式、Lint、类型、101 个测试文件的 467 项测试和生产构建全部通过。
- 第 43 个迁移已应用到独立准生产库；API、网页和 Worker 健康，浏览器实页核对未发现控制台错误。
- 准生产后台已核对人员与资格筛选、主播取消资格日期边界、化妆师与运营状态只读、人员删除提示、客服可删除及管理员不可删除。
- 主播、化妆师、运营和账号管理页的紧凑筛选栏已完成实页与 390px 窄屏回归。
- 客服账号列表的本站范围、管理员只读、本站客服账号创建及管理员越权拒绝均已纳入自动化测试。
- 桌面后台的页面滚动容器、左侧菜单独立滚动和手机断点回退已纳入样式与端到端回归。
- Playwright 8 项通过。
- 43 个数据库迁移为最新，全部数据库专项检查及 100 路并发检查通过。
- 生产依赖没有严重或高危漏洞；存在 2 个上游中危项，已记录。
- 完整证据见[全网页端迁移验收记录](全网页端迁移验收记录.md)。
- 准生产数据证据见[准生产数据准备记录](准生产数据准备记录.md)。
- 准生产角色筛选核对结果：主播 1,235、化妆师 58、运营 123、客服 2、管理员 1；总账号 1,419。

## 下一步

目标服务器应用、名单、账号和备份恢复已经完成，剩余顺序如下：

1. 修通腾讯云公网 443，仅保留 22、80、443 三个必要公网端口。
2. 在真实 Android、iPhone、微信内置浏览器及电脑浏览器完成 HTTPS 访问。
3. 由管理员创建正式客服账号，并核对客服场地范围。
4. 使用专用验收账号完成管理员、客服、主播、运营、化妆师五角色核心业务 UAT。
5. 在目标服务器隔离测试库复跑容量测试，不向正式业务库写入仿真预约。
6. 开启腾讯云系统盘自动快照和异机备份，再按单场地灰度开放。

服务器购买、云账号操作授权、正式名单、备案安排和最终业务签字必须由项目负责人提供或确认，不能由本地自动化代替。
