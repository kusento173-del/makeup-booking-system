# 全网页端迁移验收记录

> 状态：本地自动化验收完成，等待真实设备与生产环境验收
>
> 日期：2026-07-27
>
> 范围：客户端和认证渠道迁移，不重定义业务规则

## 1. 迁移结果

| 项目 | 结果 |
|---|---|
| 五类角色统一网页登录 | 已完成 |
| 主播、运营、化妆师响应式手机页面 | 已完成 |
| 客服、管理员电脑后台 | 已保留并接入统一认证 |
| 小程序和 Taro | 已从 Git、依赖、构建和部署中删除 |
| 微信登录和一次性绑定 | 已从 API、前端和配置中删除 |
| 旧绑定数据库表 | 已通过迁移删除 |
| 原预约与排班业务 API | 保持 |

## 2. 自动化验收结果

| 检查 | 结果 |
|---|---|
| 格式、ESLint、TypeScript | 通过 |
| 单元与服务测试 | 94 个文件、424 项测试通过：API 381、网页 36、Worker 7 |
| 生产构建 | API、响应式网页、Worker 和数据库包通过 |
| Playwright | 8 项通过：认证契约、登录入口、API 健康和管理员核心页面 |
| 数据库迁移 | 40 个迁移已应用，Schema 为最新 |
| 数据库专项检查 | 主数据、身份、审计、导入、班次、请假、临时不可排班、预约、固定和导出全部通过 |
| 预约并发 | 100 路同档期、每日次数、复用、回滚和原子改期通过 |
| 生产依赖安全 | 0 个严重、0 个高危；2 个上游中危 |
| Git 文件安全 | 446 个已跟踪文件检查通过，无被禁止的密钥、名单、备份或环境文件 |
| OpenAPI | 保留密码登录和账号管理；不再暴露微信登录与绑定码接口 |

端到端复测时发现登录页测试仍断言旧标题“管理后台登录”，已经改为统一网页实际标题“账号登录”并重新通过。开发态 API watch 在生产构建更新文件时曾短暂报告端口占用，不影响独立构建和复测结果；生产环境不使用 watch 进程。

## 3. 浏览器角色检查

- 主播、运营、化妆师分别通过手机视口完成登录并进入对应首页。
- 主播预约、未来排班和请假入口可访问。
- 运营负责主播、主播详情、预约和固定相关入口可访问。
- 化妆师排班、班次、请假、加班和临时不可排班入口可访问。
- 客服和管理员电脑后台继续使用原审批、排班、人员和审计功能。
- 删除旧认证后，密码登录、首次改密、角色选择、刷新会话和退出登录保持可用。

## 4. 仍需人工验收

- 真实 iPhone、Android 手机浏览器和微信内置浏览器的布局、触控和键盘行为。
- 生产域名、HTTPS、跨域、Cookie、安全头和反向代理配置。
- 五类真实业务用户的端到端签字。
- 正式名单导入后的场地、角色、运营关系和账号核对。
- 单场地灰度期间的容量、监控、备份、回滚和客服处置演练。

这些项目是生产放行条件，不应被本地浏览器或单元测试替代。

## 5. 本地残留说明

Git、依赖、构建和部署中已经没有 `apps/miniapp`。Windows 上仍可能看到一个空的 `apps/miniapp` 文件夹，这是微信开发者工具占用目录句柄所致；关闭微信开发者工具后可直接删除，不含任何代码，也不会被提交或部署。
