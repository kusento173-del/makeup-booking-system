# 化妆部预约系统上线前全量验收记录

> 状态：进行中  
> 开始日期：2026-07-26  
> 负责人：Codex（执行与记录）、业务负责人（最终业务验收）

## 1. 使用方式

本文档是本轮上线前全量测试的唯一持续记录。每完成一个阶段都必须更新：

1. 被测 Git 提交与环境；
2. 实际执行的检查和结果；
3. 发现的缺陷、风险与严重级别；
4. 对应修复提交和回归结果；
5. 下一步与尚未完成项。

任何未在本文记录中通过的项目，不得仅凭口头判断标记为已验收。

## 2. 验收范围

- 角色：主播、化妆师、运营、客服、管理员。
- 客户端：微信小程序、电脑管理后台、API、Worker。
- 业务：身份绑定、人员与负责关系、班次、预约、改期、取消、固定、请假、加班、临时不可排、排班查询、审批、导入、导出和审计。
- 非功能：权限隔离、并发一致性、幂等、事务回滚、性能容量、安全、部署、备份恢复与故障恢复。
- 数据：模拟导入现有主播、运营、化妆师及负责关系名单；固定主播历史名单仅用于数据质量检查，不导入系统。

## 3. 测试基线

### 3.1 代码与本地改动

- 初始被测提交：`15ea84e fix(miniapp): tolerate non-string route params`
- 用户本地文件：`apps/miniapp/project.config.json`
  - 状态：用户未提交改动。
  - 处理：不修改、不格式化、不暂存、不提交。
  - 影响：全仓 `pnpm check` 的 Prettier 阶段会因该文件产生误报，源码格式检查需排除此文件后独立执行。

### 3.2 运行环境

- Node.js：项目要求 `>=24.11.0 <25`
- pnpm：项目要求 `>=11.9.0 <12`
- PostgreSQL：本地 Docker，端口 `5432`，健康
- Redis：本地 Docker，端口 `6379`，健康
- API：`http://127.0.0.1:3000`
- 管理后台：`http://127.0.0.1:5173`
- 时区：Asia/Shanghai；数据库会话 UTC
- 数据库迁移：35 个，已全部应用

### 3.3 原始名单

| 文件 | 当前用途 |
|---|---|
| `data/source/线下主播人员表.xlsx` | 模拟导入、重复与空值检查 |
| `data/source/化妆师人员表.xlsx` | 模拟导入、昵称唯一性与场地检查 |
| `data/source/运营人员表.xlsx` | 模拟导入、重名与场地检查 |
| `data/source/主播与运营对应表.xlsx` | 负责关系匹配与无法匹配留空检查 |
| `data/source/化妆师固定主播名单.xlsx` | 仅做质量盘点，不导入系统 |

## 4. 阶段结果

### 4.1 仓库与自动化基线

| 检查 | 结果 | 证据摘要 |
|---|---|---|
| 测试资产盘点 | 通过 | 116 个测试文件 |
| ESLint | 通过 | 全仓无错误 |
| TypeScript | 通过 | 根工具、API、后台、小程序、Worker、数据库包全部通过 |
| 单元/服务测试 | 通过 | 471 项：API 379、后台 28、小程序 56、Worker 8 |
| 生产构建 | 通过 | API、后台、小程序、Worker、数据库包全部构建成功 |
| Playwright E2E | 通过 | 8 项 |
| Prisma schema | 通过 | 校验成功 |
| 数据库迁移 | 通过 | 35 个迁移，数据库为最新 |
| 数据库约束 | 通过 | 主数据、身份、审计、导入、班次、请假、预约、固定、导出等检查通过 |
| 基础并发 | 通过 | 100 路档期争抢、每日上限、复用、回滚及原子改期通过 |
| PostgreSQL/Redis | 通过 | 连接、时区、扩展、认证 PING 均通过 |
| 仓库敏感文件策略 | 通过 | 526 个跟踪文件，无禁止路径 |
| 全仓 Prettier | 通过（受控文件） | 排除用户本地 `project.config.json` 后，465 个受控源码和文档文件全部通过 |
| 生产依赖安全审计 | 通过（高危门禁） | 高危漏洞已清零；仍有 2 个低危、10 个中危项待风险评估 |

### 4.2 已发现问题

#### QA-SEC-001：Excel 导出依赖链存在高危拒绝服务漏洞

- 严重级别：高，上线阻断。
- 发现方式：`pnpm security:check`
- 受影响链路：`exceljs -> archiver -> minimatch -> brace-expansion`
- 当前版本：仓库存在 `brace-expansion` 1.1.16、2.1.2、5.0.7。
- 安全版本：5.0.8。
- 修复：使用 pnpm 精确覆盖将所有受影响传递版本统一为 `brace-expansion 5.0.8`，未接受自动工具提出的其他 23 个无关跨版本覆盖。
- 回归：
  - `pnpm security:check` 通过，高危漏洞由 1 个降为 0 个；
  - 依赖树仅剩 `brace-expansion 5.0.8`；
  - Excel 导出 8 个测试文件、23 项测试全部通过；
  - ESLint、全仓类型检查、471 项测试及全部生产构建再次通过。
- 状态：已修复并通过回归。

#### QA-SEC-002：构建工具链仍报告中低危依赖

- 严重级别：中低，当前不是上线阻断。
- 数量：2 个低危、10 个中危。
- 范围：
  - Webpack、esbuild、webpack-dev-server 均来自 Taro 小程序构建/开发链，不进入 API、Worker 或小程序运行时；
  - Valibot 来自 Prisma 的可选开发链；
  - UUID 来自 ExcelJS，漏洞条件为调用 v3/v5/v6 且传入外部缓冲区；本系统不调用这些接口，Excel 导出回归已通过。
- 处置：暂不使用自动工具提出的跨大版本强制覆盖；保留在发布风险清单，后续优先随 Taro、Prisma 和 ExcelJS 官方依赖升级消除。
- 状态：已评估，持续跟踪。

#### QA-API-001：取消预约响应状态与 OpenAPI 契约不一致

- 严重级别：中。
- 发现方式：隔离数据库真实 HTTP 预约闭环。
- 现象：`POST /appointments/{id}/cancel` 的文档声明 `200 OK`，实际因 NestJS 默认行为返回 `201 Created`。
- 影响：不会导致当前前端失败，但“取消既有资源”并未创建资源，错误状态会影响严格客户端、接口监控和后续集成。
- 修复：为取消接口显式设置 `@HttpCode(200)`，并增加控制器元数据回归测试。
- 回归：
  - API 79 个测试文件、380 项测试通过；
  - API TypeScript 检查与生产构建通过；
  - 真实 HTTP 取消操作返回 `200`；
  - 预约闭环和 20 路抢档回归通过。
- 状态：已修复并通过回归。

### 4.3 原始名单质量检查

| 数据集 | 行数 | 结果 |
|---|---:|---|
| 主播 | 1168 | 编号、姓名、场地完整；编号无重复；19 组重名 |
| 化妆师 | 49 | 姓名、昵称、场地完整；昵称唯一 |
| 运营 | 109 | 姓名、场地完整；姓名唯一 |
| 主播—运营关系 | 1168 | 1093 条可唯一匹配；75 条运营姓名不在运营名单 |
| 固定名单（仅参考） | 338 | 330 条同场地可匹配；8 条主播与化妆师跨场地 |

数据风险与处理：

1. 75 条无法匹配的运营关系必须留空，不允许相似名称猜测。
2. 19 组重名主播必须始终使用主播编号作为唯一标识。
3. 1168 个主播编号在源表中全部为数值单元格，迁移时必须显式转换成文本；未来带前导零的编号必须从源头按文本保存。
4. 固定名单按既定范围不导入数据库；其中 8 条跨场地记录证明不能把该表直接当作系统事实。
5. 当前五份工作簿均只有 `Sheet1` 有数据，额外两个空工作表不参与迁移。

### 4.4 隔离数据库迁移演练

- 隔离数据库：`makeup_booking_qa_20260726`
- 数据库迁移：35 个迁移全部应用，未使用开发库或生产库。
- 首次演练发现迁移已预置三个场地，演练脚本重复创建场地触发唯一键保护；事务完整回滚，业务表保持为空。
- 修正演练脚本为复用预置场地后，全量导入成功。

| 核对项 | 预期 | 实际 | 结果 |
|---|---:|---:|---|
| 主播 | 1168 | 1168 | 通过 |
| 化妆师 | 49 | 49 | 通过 |
| 运营 | 109 | 109 | 通过 |
| 主播—运营关系 | 1093 | 1093 | 通过 |
| 未分配运营主播 | 75 | 75 | 通过 |
| 导入批次 | 4 | 4（全部 `SUCCEEDED`） | 通过 |
| 导入明细 | 2494 | 2494 | 通过 |
| 有效创建明细 | 2419 | 2419 | 通过 |
| 警告且不变更明细 | 75 | 75 | 通过 |
| 跨场地负责关系 | 0 | 0 | 通过 |
| 负责关系日期异常 | 0 | 0 | 通过 |
| 固定规则 | 0 | 0 | 通过 |
| 预约 | 0 | 0 | 通过 |

结论：

1. 无法匹配的 75 条关系已按要求保留为空，同时留存可追溯的导入警告。
2. 所有已建立负责关系均同场地，开始日期统一为本次确认日期 `2026-07-26`，结束日期为空。
3. 固定主播参考名单未进入数据库，没有把历史参考数据误当作有效固定关系。
4. 原始名单已可作为后续五角色业务、容量和权限测试的真实数据基线。

### 4.5 五角色权限与预约核心闭环

隔离环境：

- API：`http://127.0.0.1:3100`
- 测试身份：管理员、松江/现厂客服、编号 `000001`/`000002` 主播、松江/现厂运营、松江/现厂化妆师。
- 真实名单与测试身份并存，测试令牌仅存放在 Git 忽略的临时目录。

| 范围 | 结果 | 证据摘要 |
|---|---|---|
| 身份有效性 | 通过 | 9 个角色/场地身份均由数据库会话和签名访问令牌通过 `/auth/me` 验证 |
| 数据权限 | 通过 | 运营仅见负责主播；主播仅见本站化妆师；化妆师不能读取主播名单；客服不能管理后台账号 |
| 场地权限 | 通过 | 客服跨场地排班、班次和运营代跨场地主播操作均返回 403 |
| 班次 | 通过 | 两地化妆师首次设置、本人读取、本站客服读取、跨场地拒绝均通过 |
| 预约边界 | 通过 | D+1～D+7、15/30/45/60 分钟及跨场地约束通过 |
| 单日次数 | 通过 | 首次成功；第二次需明确确认；确认后成功；第三次被拒绝 |
| 幂等 | 通过 | 相同键相同内容重放原结果；相同键不同内容被拒绝 |
| 改期与取消 | 通过 | 原预约转已取消、新预约创建；越权与过期版本被拒绝；取消后档期恢复 |
| HTTP 抢档 | 通过 | 20 路同时请求同一化妆师同一档期，仅 1 路成功、19 路冲突 |

## 5. 尚未完成

- [x] 修复 QA-SEC-001 并完成回归。
- [x] 排除用户本地配置后完成全量源码格式门禁。
- [x] 对五份 Excel 做完整数据质量分析与隔离数据库模拟导入。
- [ ] 补齐五角色真实 API 业务闭环及权限矩阵自动化。
- [ ] 补齐管理后台真实客服/管理员流程 E2E。
- [ ] 完成小程序逐页冒烟和主播、运营、化妆师真实流程。
- [ ] 完成跨端状态同步、过期会话和异常恢复测试。
- [ ] 完成 1500 单/日容量、高并发抢占、峰值性能和长稳测试。
- [ ] 完成越权、输入、速率限制、敏感数据与依赖安全检查。
- [ ] 完成部署配置、Worker、备份与恢复演练。
- [ ] 输出最终缺陷清单、剩余风险和上线结论。

## 6. 下一步

1. 在隔离数据库创建五角色测试身份与覆盖两场地的业务样本。
2. 执行身份绑定、权限矩阵及主播—运营—化妆师预约闭环测试。
3. 覆盖固定、改单、取消、请假、加班、临时不可排、审批和排班查询。
