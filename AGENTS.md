# 项目协作规范

## 代码原则

- 所有代码必须精简、高效、易读、易测试、易维护；结构清晰、职责单一，不增加与需求无关的功能。
- 采用清晰的分层和模块边界，业务规则集中管理，避免循环依赖、重复实现和跨层调用。
- 优先采用模块化单体和项目已经选定的技术栈；没有明确收益时，不新增框架、中间件、抽象层或微服务。
- 公共抽象只在确有重复且语义稳定时提取，避免过度设计；性能优化必须基于真实瓶颈，同时保证代码可理解性。
- 新增或修改复杂业务逻辑时必须补充相应测试；重构不得改变既有业务行为，除非需求明确要求。
- 预约并发、每日次数和权限规则必须由服务端及数据库兜底，不能只做前端校验。
- 人员名单、密钥、数据库备份和包含隐私的数据不得提交到 Git。

## 业务依据

- 开发前同时查阅产品需求、业务规则与状态流转、角色权限矩阵、数据字典与数据库设计；不能只根据单个页面或接口猜测规则。
- 业务规则和权限必须在领域服务中保持唯一实现，前端提示、定时任务和导入脚本复用同一规则定义。
- 现有固定主播名单只作历史参考，不导入数据库；无法匹配运营主档的关系按未分配处理。
- 文档之间出现矛盾时先统一文档和测试，再实现代码，不保留两套行为。

## Git规则

- 开始修改前先检查 `git status` 和现有未提交改动。
- 每完成一个完整的代码或文档变更，运行相应测试或检查后创建一次 Git 提交。
- 一次提交只包含一个相关逻辑变更，不混入无关文件。
- 提交信息使用 `feat:`、`fix:`、`docs:`、`refactor:`、`test:` 或 `chore:` 前缀。
- 提交前检查 `git diff --staged`，确保没有 `.env`、Excel人员名单或其他敏感数据。
- 完成后向用户报告提交哈希和已运行的测试。
- 如果测试失败或改动尚未完成，不要将其标记为完成版本提交。
